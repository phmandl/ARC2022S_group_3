#!/usr/bin/env python3
PACKAGE = "wall_follow"

from dynamic_reconfigure.parameter_generator_catkin import *

gen = ParameterGenerator()

# gen.add("int_param",    int_t,    0, "An Integer parameter", 50,  0, 100)
gen.add("Kp", double_t, 0, "PID proportional gain", 1, 0,  5)
gen.add("Ki", double_t, 0, "PID integral gain",  0.1, 0,   1)
gen.add("Kd", double_t, 0, "PID derivative gain", 0, 0,   1)
gen.add("maxVelo", double_t, 0, "maxmimum vehicle speed", 1.5, 0, 5)
gen.add("N", int_t, 0, "Filter Coeff.",    100, 0,   1000)
# gen.add("str_param",    str_t,    0, "A string parameter",  "Hello World")
# gen.add("bool_param",   bool_t,   0, "A Boolean parameter",  True)

# size_enum = gen.enum([ gen.const("Small",      int_t, 0, "A small constant"),
#                        gen.const("Medium",     int_t, 1, "A medium constant"),
#                        gen.const("Large",      int_t, 2, "A large constant"),
#                        gen.const("ExtraLarge", int_t, 3, "An extra large constant")],
#                      "An enum to set size")

# gen.add("size", int_t, 0, "A size parameter which is edited via an enum", 1, 0, 3, edit_method=size_enum)

exit(gen.generate(PACKAGE, "wall_follow", "dyn_reconfig"))